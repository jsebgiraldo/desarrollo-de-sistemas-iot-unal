# Usa la imagen oficial del gateway de ThingsBoard
FROM thingsboard/tb-gateway:latest

# Copia el archivo de configuración personalizado (si es necesario)
COPY thingsboard_gateway.yaml /thingsboard_gateway/config/

# Copia el archivo de dispositivos (opcional, si tu integración lo requiere)
# COPY devices.json /thingsboard_gateway/config/

# El entrypoint y el comando ya están definidos en la imagen oficial
